# Local development: builds the image from the Dockerfile in this repo.
version: '3.8'

services:
  youtube-downloader:
    build: .
    ports:
      - "3837:3837"
    volumes:
      - ./downloads:/app/downloads
    environment:
      - PORT=3837
    restart: unless-stopped
