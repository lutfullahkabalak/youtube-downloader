version: '3.8'

services:
  youtube-downloader:
    image: ghcr.io/lutfullahkabalak/youtube-downloader:latest
    ports:
      - "3837:3837"
    volumes:
      - ./downloads:/app/downloads
    environment:
      - PORT=3837
    restart: unless-stopped
